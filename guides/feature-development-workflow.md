# Feature Development Workflow

```text
Understand -> Plan -> Implement -> Test -> Review -> Secure -> Document -> Commit -> PR -> Handover
```

For large features, save a plan under `docs/plans/<task-name>.md` before implementation.

The AI agent should not modify code until it understands scope, affected modules, risks, and acceptance criteria.
